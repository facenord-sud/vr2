<%= simple_form_for(@carreer) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :medals %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
